fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.6.1
sqlalchemy==2.0.36
alembic==1.14.0
python-multipart==0.0.19

# PostgreSQL (production database — see backend/DATABASE.md). SQLite stays
# the zero-config default for local dev; psycopg only needs to be
# installed, DATABASE_URL selects the dialect at runtime.
psycopg[binary]==3.2.3

# Redis (only used when QUEUE_BACKEND=redis, see app/queue/ — FASE G).
# Importing this package is always safe even when Redis is never used.
redis==5.2.1

# OCR
pytesseract==0.3.13
Pillow==11.0.0
opencv-python-headless==4.10.0.84
PyMuPDF==1.25.1

# HTTP client — Google OAuth token/userinfo calls (app/services/auth/google_oauth.py)
# at runtime, and the test suite's TestClient at test time.
httpx==0.28.1

# Password hashing (PROMPT 4 — local/password login, app/core/password.py).
# bcrypt directly, not passlib: passlib's last release predates bcrypt 4.x's
# API change.
bcrypt==4.2.1

# Exportação de relatórios auxiliares (FASE J) — nunca o layout oficial
# do TSE/CONTA+JE, ver app/services/reports/export_service.py.
openpyxl==3.1.5
reportlab==5.0.1

# Tests
pytest==8.3.4
pypdf==6.18.1  # test-only: verifying generated PDF report content
